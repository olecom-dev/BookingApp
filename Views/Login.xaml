<Page
    x:Class="BookingApp.Views.Login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BookingApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Height="600" Width="400">

    <Grid>
        <StackPanel Margin="30,150,30,100">
            <TextBlock Name="lbUsername" Text="Benutzername" FontSize="30" VerticalAlignment="Center"></TextBlock>
            <TextBox Name="Username" Margin="0,30,30,0"></TextBox>
            <TextBlock Name="lbPassword" Text="Passwort" FontSize="30" VerticalAlignment="Center" Margin="0,60,30,0"></TextBlock>
            <PasswordBox Name="Passwort"></PasswordBox>
        </StackPanel>
    </Grid>
</Page>
